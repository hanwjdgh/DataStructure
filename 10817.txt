#include <stdio.h>

int main(){
	int arr[3];
	int a,b,tmp;
	for (a = 0; a < 3; a++){
		scanf("%d", &arr[a]);
	}
	for (a = 0; a < 3; a++){
		for (b = 0; b < 2; b++){
			if (arr[b] > arr[b + 1]){
				tmp = arr[b];
				arr[b] = arr[b + 1];
				arr[b + 1] = tmp;
			}
		}
	}
	printf("%d", arr[1]);
	return 0;
}
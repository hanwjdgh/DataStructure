#include <stdio.h>
#include <string.h>

int main(void){
	int tcase,ret;
	int a,b,c;
	char str[21];
	scanf("%d", &tcase);
	for (a = 0; a < tcase; a++){
		scanf("%d", &ret); getchar();
		scanf("%s", str);
		for (b = 0; b < strlen(str); b++){
			for (c = 0; c < ret; c++)
				printf("%c", str[b]);
		}
		printf("\n");
	}
	return 0;
}
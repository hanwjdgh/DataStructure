#include <stdio.h>
#include <string.h>

int main(void){
	char str[1000001];
	int a,cnt=0;
	gets(str);
	for (a = 0; a < strlen(str); a++){
		if (str[a] == ' '&&str[a+1]!=' ')
			cnt++;
	}
	if (str[0] == ' ')
		--cnt;
	if (str[strlen(str)-1] == ' ')
		--cnt;
	printf("%d", cnt+1);
	return 0;
}
#include <iostream>
#include <ios>
#include "LinkedListDQ.h"

using namespace std;

int main(){
    cin.tie(NULL);
	cout.tie(NULL);
	ios::sync_with_stdio(false);

    DQueue queue;
    
    init(&queue);
    
    return 0;
}